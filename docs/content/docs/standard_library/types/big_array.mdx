---
title: "big_array"
description: "big_array.tolk standard library file"
---

import { SourceCodeLink } from '@/components/SourceCodeLink';

Module defining `BigArray<T>`

## Definitions

## `BigArray`

```tolk
struct BigArray<T> {
    private arr: [array<array<T>>, int]
}
```

`BigArray<T>` is a chunked array implementation for cases when regular `array<T>`
capacity (`0..255`) is not enough.

Internally it stores items in bins, where each bin is `array<T>` with up to 255 items.
New bins are created lazily on demand.
Total capacity is up to `255 * 255 = 65025` items.

Example:
```tolk
var list = BigArray<int>.createEmpty();
list.push(10);
list.push(20);
val first = list.first(); // 10
val last = list.last();   // 20
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/big_array.tolk#L20" />

## `BigArray<T>.createEmpty`

```tolk
fun BigArray<T>.createEmpty(): BigArray<T>
```

Creates an empty [BigArray](#bigarray).

Use this constructor to initialize a new chunked array.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/big_array.tolk#L27" />

## `BigArray<T>.createFromArray`

```tolk
fun BigArray<T>.createFromArray(items: array<T>): BigArray<T>
```

Creates [BigArray](#bigarray) and fills it with all elements from [items](#bigarray\<t>createfromarray).

Elements preserve the original order from [items](#bigarray\<t>createfromarray).
All elements are copied into the first bin directly.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/big_array.tolk#L37" />

## `BigArray<T>.first`

```tolk
fun BigArray<T>.first(self): T
```

Returns the first element of a non-empty [BigArray](#bigarray).

Fails if the array is empty.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/big_array.tolk#L50" />

## `BigArray<T>.size`

```tolk
fun BigArray<T>.size(self): int
```

Returns current number of elements in [BigArray](#bigarray).

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/big_array.tolk#L56" />

## `BigArray<T>.last`

```tolk
fun BigArray<T>.last(self): T
```

Returns the last element of a non-empty [BigArray](#bigarray).

Fails if the array is empty.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/big_array.tolk#L64" />

## `BigArray<T>.push`

```tolk
fun BigArray<T>.push(mutate self, value: T): void
```

Appends [value](#bigarray\<t>push) to the end of [BigArray](#bigarray).

Fails if array capacity exceeds 65025 elements.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/big_array.tolk#L74" />

## `BigArray<T>.pop`

```tolk
fun BigArray<T>.pop(mutate self): void
```

Removes the last element from [BigArray](#bigarray).

Fails if the array is empty.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/big_array.tolk#L97" />

## `BigArray<T>.set`

```tolk
fun BigArray<T>.set(mutate self, value: T, index: int): void
```

Replaces element at [index](#bigarray\<t>set) with [value](#bigarray\<t>push).

Fails if [index](#bigarray\<t>set) is out of bounds.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/big_array.tolk#L115" />

## `BigArray<T>.get`

```tolk
fun BigArray<T>.get(self, index: int): T
```

Returns element at [index](#bigarray\<t>set).

Fails if [index](#bigarray\<t>set) is out of bounds.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/big_array.tolk#L129" />

