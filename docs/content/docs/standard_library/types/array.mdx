---
title: "array"
description: "array.tolk standard library file"
---

import { SourceCodeLink } from '@/components/SourceCodeLink';

{/* @generated by `acton docgen`. Do not edit directly. */}

Calls [f] for every element in the array.

```tolk
val nums: array<int> = [1, 2, 3];
nums.each(fun(x: int) {
    println(x);
});
```

## Definitions

## `array<T>.each`

```tolk
fun array<T>.each(self, f: (T -> void)): void
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/types/array.tolk#L9" />

## `array<T>.map`

```tolk
fun array<T>.map<U>(self, f: (T -> U)): array<U>
```

Returns a new array with [f](#array\<t>each) applied to every element.

```tolk
val nums: array<int> = [1, 2, 3];
val doubled = nums.map<int>(fun(x: int): int { return x * 2; });
// [2, 4, 6]
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/types/array.tolk#L24" />

## `array<T>.filter`

```tolk
fun array<T>.filter(self, f: (T -> bool)): array<T>
```

Returns a new array containing only elements for which [f](#array\<t>each) returns `true`.

```tolk
val nums: array<int> = [1, 2, 3, 4];
val even = nums.filter(fun(x: int): bool { return x % 2 == 0; });
// [2, 4]
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/types/array.tolk#L41" />

