---
title: "transaction"
description: "transaction.tolk standard library file"
---

import { SourceCodeLink } from '@/components/SourceCodeLink';

Module defining transaction types.

This module provides a complete representation of TON transactions as they appear in the blockchain.
It includes structures for all phases of transaction execution: storage, credit, compute, action, and bounce.

Examples:
```tolk
val tx = results.at(0).tx.load();

// Check gas usage
val gas = tx.getUsedGas();
println1("Gas used: {}", gas);

// Inspect account status change
val description = tx.description.load();
if (description is TransOrd) {
    val compute = description.compute_ph;
    if (compute is TrComputeVm) {
        println1("VM exit code: {}", compute.exit_code);
    }
}
```

## Definitions

## `Uninitialized`

```tolk
struct (0b00) Uninitialized
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L28" />

## `Frozen`

```tolk
struct (0b01) Frozen
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L29" />

## `Active`

```tolk
struct (0b10) Active
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L30" />

## `NonExisting`

```tolk
struct (0b11) NonExisting
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L31" />

## `DepthBalanceInfo`

```tolk
struct DepthBalanceInfo {
    split_depth: uint8, // #<= 30
    balance: CurrencyCollection
}
```

Represents depth and balance information for split accounts.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L37" />

## `Transaction`

```tolk
struct (0b0111) Transaction {
    /// Account address as 256-bit hash.
    account_addr: bits256,
    /// Logical time of the transaction.
    lt: uint64,
    /// Hash of the previous transaction.
    prev_trans_hash: bits256,
    /// Logical time of the previous transaction.
    prev_trans_lt: uint64,
    /// Current time (Unix timestamp).
    now: uint32,
    /// Number of outgoing messages.
    outmsg_cnt: uint15,
    /// Account status before transaction.
    orig_status: AccountStatus,
    /// Account status after transaction.
    end_status: AccountStatus,
    /// In and out messages.
    messages: Cell<TransactionMessages>,
    /// Total fees collected.
    total_fees: CurrencyCollection,
    /// State update information.
    state_update: Cell<HashUpdate<Account>>,
    /// Transaction description.
    description: Cell<TransactionDescr>
}
```

Represents a transaction in the TON blockchain.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L43" />

## `Transaction.loadBody`

```tolk
fun Transaction.loadBody<Msg>(self)
```

Loads the in message body of the transaction with given type.

Note that if message type mismatch, loading will fail with exit code 63.

Example:
```tolk
val result = net.send(address, msg, SEND_MODE_PAY_FEES_SEPARATELY);
val tx = result.findTransaction<IncreaseCounter>({ to: address }).unwrap();
val body = tx.loadBody<IncreaseCounter>();
println(body.increaseBy);
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L81" />

## `Transaction.loadInMsg`

```tolk
fun Transaction.loadInMsg<Msg>(self)
```

Loads the in message of the transaction with given type.

Note that if message type mismatch, loading will fail with exit code 63.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L90" />

## `Transaction.getUsedGas`

```tolk
fun Transaction.getUsedGas(self)
```

Returns consumed gas for transaction.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L96" />

## `Transaction.getActionFee`

```tolk
fun Transaction.getActionFee(self): Maybe<coins>
```

Returns fee for action phase of the transaction.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L110" />

## `Transaction.getAccountAddress`

```tolk
fun Transaction.getAccountAddress(self, workchain: int = BASECHAIN)
```

Return address of account of this transaction.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L132" />

## `TransactionMessages`

```tolk
struct TransactionMessages {
    /// In message.
    in_msg: Maybe<Cell<MessageRelaxedGeneric>>,
    /// Out messages.
    out_msgs: map<uint15, Cell<MessageRelaxedGeneric>>
}
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L141" />

## `MerkleUpdate`

```tolk
struct (0b0100) MerkleUpdate<X> {
    /// Old hash.
    old_hash: bits256,
    /// New hash.
    new_hash: bits256,
    /// Old depth.
    old_depth: uint16,
    /// New depth.
    new_depth: uint16,
    /// Old value.
    old: X,
    /// New value.
    new: X
}
```

Represents merkle update for proof-of-work.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L149" />

## `HashUpdate`

```tolk
struct (0b01110010) HashUpdate<X> {
    /// Old hash.
    old_hash: bits256,
    /// New hash.
    new_hash: bits256
}
```

Represents simple hash update.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L165" />

## `MerkleProof`

```tolk
struct (0b0011) MerkleProof<X> {
    /// Virtual hash.
    virtual_hash: bits256,
    /// Depth.
    depth: uint16,
    /// Virtual root.
    virtual_root: X
}
```

Represents merkle proof.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L173" />

## `TrStoragePhase`

```tolk
struct TrStoragePhase {
    /// Storage fees collected.
    storage_fees_collected: coins,
    /// Storage fees due (if any).
    storage_fees_due: Maybe<coins>,
    /// Account status change.
    status_change: AccStatusChange
}
```

Represents storage phase of transaction processing.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L186" />

## `AccUnchanged`

```tolk
struct (0b0) AccUnchanged
```

Represents account status changes.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L196" />

## `AccFrozen`

```tolk
struct (0b10) AccFrozen
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L197" />

## `AccDeleted`

```tolk
struct (0b11) AccDeleted
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L198" />

## `TrCreditPhase`

```tolk
struct TrCreditPhase {
    /// Due fees collected (if any).
    due_fees_collected: Maybe<coins>,
    /// Credit amount.
    credit: CurrencyCollection
}
```

Represents credit phase of transaction processing.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L204" />

## `TrComputeSkipped`

```tolk
struct (0b0) TrComputeSkipped {
    reason: ComputeSkipReason
}
```

Represents compute phase of transaction processing.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L212" />

## `TrComputeVm`

```tolk
struct (0b1) TrComputeVm {
    success: bool,
    msg_state_used: bool,
    account_activated: bool,
    gas_fees: coins,
    data: Cell<TrComputeVmRef>
}
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L215" />

## `TrComputeVmRef`

```tolk
struct TrComputeVmRef {
    gas_used: VarUint7,
    gas_limit: VarUint7,
    gas_credit: Maybe<VarUint3>,
    mode: int8,
    exit_code: int32,
    exit_arg: Maybe<int32>,
    vm_steps: uint32,
    vm_init_state_hash: bits256,
    vm_final_state_hash: bits256
}
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L223" />

## `CSkipNoState`

```tolk
struct (0b00) CSkipNoState
```

Represents reasons for skipping computation.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L239" />

## `CSkipBadState`

```tolk
struct (0b01) CSkipBadState
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L240" />

## `CSkipNoGas`

```tolk
struct (0b10) CSkipNoGas
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L241" />

## `CSkipSuspended`

```tolk
struct (0b110) CSkipSuspended
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L242" />

## `TrActionPhase`

```tolk
struct TrActionPhase {
    success: bool,
    valid: bool,
    no_funds: bool,
    status_change: AccStatusChange,
    total_fwd_fees: Maybe<coins>,
    total_action_fees: Maybe<coins>,
    result_code: int32,
    result_arg: Maybe<int32>,
    tot_actions: uint16,
    spec_actions: uint16,
    skipped_actions: uint16,
    msgs_created: uint16,
    action_list_hash: bits256,
    tot_msg_size: StorageUsed
}
```

Represents action phase of transaction processing.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L248" />

## `TrBounceNegfunds`

```tolk
struct (0b00) TrBounceNegfunds
```

Represents bounce phase of transaction processing.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L266" />

## `TrBounceNofunds`

```tolk
struct (0b01) TrBounceNofunds {
    msg_size: StorageUsed,
    req_fwd_fees: coins
}
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L267" />

## `TrBounceOk`

```tolk
struct (0b1) TrBounceOk {
    msg_size: StorageUsed,
    msg_fees: coins,
    fwd_fees: coins
}
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L271" />

## `TransOrd`

```tolk
@overflow1023_policy("suppress")
struct (0b0000) TransOrd {
    credit_first: bool,
    storage_ph: Maybe<TrStoragePhase>,
    credit_ph: Maybe<TrCreditPhase>,
    compute_ph: TrComputePhase,
    action: Maybe<Cell<TrActionPhase>>,
    aborted: bool,
    bounce: Maybe<TrBouncePhase>,
    destroyed: bool
}
```

Represents ordinary transaction description.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L281" />

## `TransStorage`

```tolk
struct (0b0001) TransStorage {
    storage_ph: TrStoragePhase
}
```

Represents storage transaction description.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L294" />

## `TransTickTock`

```tolk
@overflow1023_policy("suppress")
struct (0b001) TransTickTock {
    is_tock: bool,
    storage_ph: TrStoragePhase,
    compute_ph: TrComputePhase,
    action: Maybe<Cell<TrActionPhase>>,
    aborted: bool,
    destroyed: bool
}
```

Represents tick-tock transaction description.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L299" />

## `SplitMergeInfo`

```tolk
struct SplitMergeInfo {
    cur_shard_pfx_len: uint8, // ## 6
    acc_split_depth: uint8,   // ## 6
    this_addr: bits256,
    sibling_addr: bits256
}
```

Represents split-merge information.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L310" />

## `TransSplitPrepare`

```tolk
@overflow1023_policy("suppress")
struct (0b0100) TransSplitPrepare {
    split_info: SplitMergeInfo,
    storage_ph: Maybe<TrStoragePhase>,
    compute_ph: TrComputePhase,
    action: Maybe<Cell<TrActionPhase>>,
    aborted: bool,
    destroyed: bool
}
```

Represents split prepare transaction description.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L318" />

## `TransSplitInstall`

```tolk
struct (0b0101) TransSplitInstall {
    split_info: SplitMergeInfo,
    prepare_transaction: Cell<Transaction>,
    installed: bool
}
```

Represents split install transaction description.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L329" />

## `TransMergePrepare`

```tolk
struct (0b0110) TransMergePrepare {
    split_info: SplitMergeInfo,
    storage_ph: TrStoragePhase,
    aborted: bool
}
```

Represents merge prepare transaction description.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L336" />

## `TransMergeInstall`

```tolk
@overflow1023_policy("suppress")
struct (0b0111) TransMergeInstall {
    split_info: SplitMergeInfo,
    prepare_transaction: Cell<Transaction>,
    storage_ph: Maybe<TrStoragePhase>,
    credit_ph: Maybe<TrCreditPhase>,
    compute_ph: TrComputePhase,
    action: Maybe<Cell<TrActionPhase>>,
    aborted: bool,
    destroyed: bool
}
```

Represents merge install transaction description.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L343" />

## `VarUint7.packToBuilder`

```tolk
fun VarUint7.packToBuilder(self, mutate b: builder)
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L367" />

## `VarUint7.unpackFromSlice`

```tolk
fun VarUint7.unpackFromSlice(mutate s: slice): VarUint7
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L371" />

## `VarUint3.packToBuilder`

```tolk
fun VarUint3.packToBuilder(self, mutate b: builder)
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L378" />

## `VarUint3.unpackFromSlice`

```tolk
fun VarUint3.unpackFromSlice(mutate s: slice): VarUint3
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L382" />

## `StorageUsed`

```tolk
struct StorageUsed {
    cells: VarUint7,
    bits: VarUint7
}
```

Represents storage used information.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L388" />

## `StorageExtraNone`

```tolk
struct (0b000) StorageExtraNone
```

Represents storage extra information.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L394" />

## `StorageExtraDict`

```tolk
struct (0b001) StorageExtraDict {
    dict_hash: uint256
}
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L395" />

## `StorageInfo`

```tolk
struct StorageInfo {
    /// Storage used.
    used: StorageUsed,
    /// Storage extra information.
    storage_extra: StorageExtraInfo,
    /// Last paid time.
    last_paid: uint32,
    /// Due payment (optional).
    due_payment: Maybe<coins>
}
```

Represents storage information.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L403" />

## `AccountNone`

```tolk
struct (0b0) AccountNone
```

Represents account.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L415" />

## `AccountInfo`

```tolk
struct (0b1) AccountInfo {
    /// Account address.
    addr: address,
    /// Storage statistics.
    storage_stat: StorageInfo,
    /// Account storage.
    storage: AccountStorage
}
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L416" />

## `AccountStorage`

```tolk
struct AccountStorage {
    /// Last transaction logical time.
    last_trans_lt: uint64,
    /// Account balance.
    balance: CurrencyCollection,
    /// Account state.
    state: AccountStatus
}
```

Represents account storage.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L429" />

