---
title: "transaction"
description: "transaction.tolk standard library file"
---

import { SourceCodeLink } from '@/components/SourceCodeLink';

Module defining transaction types.

This module provides a complete representation of TON transactions as they appear in the blockchain.
It includes structures for all phases of transaction execution: storage, credit, compute, action, and bounce.

Examples:
```tolk
val tx = results.at(0).tx.load();

// Check gas usage
val gas = tx.getUsedGas();
println1("Gas used: {}", gas);

// Inspect account status change
val description = tx.description.load();
if (description is TransOrd) {
    val compute = description.computePh;
    if (compute is TrComputeVm) {
        println1("VM exit code: {}", compute.exitCode);
    }
}
```

## Definitions

## `Uninitialized`

```tolk
struct (0b00) Uninitialized
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L28" />

## `Frozen`

```tolk
struct (0b01) Frozen
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L29" />

## `Active`

```tolk
struct (0b10) Active
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L30" />

## `NonExisting`

```tolk
struct (0b11) NonExisting
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L31" />

## `DepthBalanceInfo`

```tolk
struct DepthBalanceInfo {
    splitDepth: uint8, // #<= 30
    balance: CurrencyCollection
}
```

Represents depth and balance information for split accounts.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L38" />

## `Transaction`

```tolk
struct (0b0111) Transaction {
    /// Account address as 256-bit hash.
    accountAddr: bits256,
    /// Logical time of the transaction.
    lt: uint64,
    /// Hash of the previous transaction.
    prevTransHash: bits256,
    /// Logical time of the previous transaction.
    prevTransLt: uint64,
    /// Current time (Unix timestamp).
    now: uint32,
    /// Number of outgoing messages.
    outmsgCnt: uint15,
    /// Account status before transaction.
    origStatus: AccountStatus,
    /// Account status after transaction.
    endStatus: AccountStatus,
    /// In and out messages.
    messages: Cell<TransactionMessages>,
    /// Total fees collected.
    totalFees: CurrencyCollection,
    /// State update information.
    stateUpdate: Cell<HashUpdate<Account>>,
    /// Transaction description.
    description: Cell<TransactionDescr>
}
```

Represents a transaction in the TON blockchain.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L44" />

## `Transaction.loadBody`

```tolk
fun Transaction.loadBody<Msg>(self)
```

Loads the in message body of the transaction with given type.

Fails if transaction has no inbound message (`messages.inMsg` is `None`), because this
method unwraps it before typed decoding.

After an inbound message exists, if message type does not match `Msg`, loading fails with
exit code 63.

Example:
```tolk
val result = net.send(address, msg);
val tx = result.findTransaction<IncreaseCounter>({ to: address }).unwrap();
val body = tx.loadBody<IncreaseCounter>();
println(body.increaseBy);
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L86" />

## `Transaction.loadInMsg`

```tolk
fun Transaction.loadInMsg<Msg>(self)
```

Loads the in message of the transaction with given type.

Fails if transaction has no inbound message (`messages.inMsg` is `None`), because this
method unwraps it before typed decoding.

After an inbound message exists, if message type does not match `Msg`, loading fails with
exit code 63.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L99" />

## `Transaction.getUsedGas`

```tolk
fun Transaction.getUsedGas(self)
```

Returns consumed gas for ordinary transactions with VM compute phase.

Fails with [Assert.fail](../../testing/assert/#assertfail):
- if `description` is not [TransOrd](#transord);
- if `description.computePh` is [TrComputeSkipped](#trcomputeskipped).

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L109" />

## `Transaction.getActionFee`

```tolk
fun Transaction.getActionFee(self): Maybe<coins>
```

Returns action-phase fee based on Transaction.description variant.

Behavior by variant:
- [TransOrd](#transord) and [TransTickTock](#transticktock) with `action = Some`: returns `action.totalActionFees`;
- [TransOrd](#transord) and [TransTickTock](#transticktock) with `action = None`: returns `None`;
- all other TransactionDescr variants: returns `None`.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L128" />

## `Transaction.getAccountAddress`

```tolk
fun Transaction.getAccountAddress(self, workchain: int = BASECHAIN)
```

Return address of account of this transaction.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L150" />

## `TransactionMessages`

```tolk
struct TransactionMessages {
    /// In message.
    inMsg: Maybe<Cell<MessageRelaxedGeneric>>,
    /// Out messages.
    outMsgs: map<uint15, Cell<MessageRelaxedGeneric>>
}
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L159" />

## `MerkleUpdate`

```tolk
struct (0b0100) MerkleUpdate<X> {
    /// Old hash.
    oldHash: bits256,
    /// New hash.
    newHash: bits256,
    /// Old depth.
    oldDepth: uint16,
    /// New depth.
    newDepth: uint16,
    /// Old value.
    old: X,
    /// New value.
    new: X
}
```

Represents merkle update for proof-of-work.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L167" />

## `HashUpdate`

```tolk
struct (0b01110010) HashUpdate<X> {
    /// Old hash.
    oldHash: bits256,
    /// New hash.
    newHash: bits256
}
```

Represents simple hash update.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L183" />

## `MerkleProof`

```tolk
struct (0b0011) MerkleProof<X> {
    /// Virtual hash.
    virtualHash: bits256,
    /// Depth.
    depth: uint16,
    /// Virtual root.
    virtualRoot: X
}
```

Represents merkle proof.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L191" />

## `TrStoragePhase`

```tolk
struct TrStoragePhase {
    /// Storage fees collected.
    storageFeesCollected: coins,
    /// Storage fees due (if any).
    storageFeesDue: Maybe<coins>,
    /// Account status change.
    statusChange: AccStatusChange
}
```

Represents storage phase of transaction processing.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L204" />

## `AccUnchanged`

```tolk
struct (0b0) AccUnchanged
```

Represents account status changes.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L214" />

## `AccFrozen`

```tolk
struct (0b10) AccFrozen
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L215" />

## `AccDeleted`

```tolk
struct (0b11) AccDeleted
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L216" />

## `TrCreditPhase`

```tolk
struct TrCreditPhase {
    /// Due fees collected (if any).
    dueFeesCollected: Maybe<coins>,
    /// Credit amount.
    credit: CurrencyCollection
}
```

Represents credit phase of transaction processing.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L222" />

## `TrComputeSkipped`

```tolk
struct (0b0) TrComputeSkipped {
    reason: ComputeSkipReason
}
```

Represents compute phase of transaction processing.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L230" />

## `TrComputeVm`

```tolk
struct (0b1) TrComputeVm {
    success: bool,
    msgStateUsed: bool,
    accountActivated: bool,
    gasFees: coins,
    data: Cell<TrComputeVmRef>
}
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L233" />

## `TrComputeVmRef`

```tolk
struct TrComputeVmRef {
    gasUsed: VarUint7,
    gasLimit: VarUint7,
    gasCredit: Maybe<VarUint3>,
    mode: int8,
    exitCode: int32,
    exitArg: Maybe<int32>,
    vmSteps: uint32,
    vmInitStateHash: bits256,
    vmFinalStateHash: bits256
}
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L241" />

## `CSkipNoState`

```tolk
struct (0b00) CSkipNoState
```

Represents reasons for skipping computation.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L257" />

## `CSkipBadState`

```tolk
struct (0b01) CSkipBadState
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L258" />

## `CSkipNoGas`

```tolk
struct (0b10) CSkipNoGas
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L259" />

## `CSkipSuspended`

```tolk
struct (0b110) CSkipSuspended
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L260" />

## `TrActionPhase`

```tolk
struct TrActionPhase {
    success: bool,
    valid: bool,
    noFunds: bool,
    statusChange: AccStatusChange,
    totalFwdFees: Maybe<coins>,
    totalActionFees: Maybe<coins>,
    resultCode: int32,
    resultArg: Maybe<int32>,
    totActions: uint16,
    specActions: uint16,
    skippedActions: uint16,
    msgsCreated: uint16,
    actionListHash: bits256,
    totMsgSize: StorageUsed
}
```

Represents action phase of transaction processing.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L266" />

## `TrBounceNegfunds`

```tolk
struct (0b00) TrBounceNegfunds
```

Represents bounce phase of transaction processing.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L284" />

## `TrBounceNofunds`

```tolk
struct (0b01) TrBounceNofunds {
    msgSize: StorageUsed,
    reqFwdFees: coins
}
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L285" />

## `TrBounceOk`

```tolk
struct (0b1) TrBounceOk {
    msgSize: StorageUsed,
    msgFees: coins,
    fwdFees: coins
}
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L289" />

## `TransOrd`

```tolk
@overflow1023_policy("suppress")
struct (0b0000) TransOrd {
    creditFirst: bool,
    storagePh: Maybe<TrStoragePhase>,
    creditPh: Maybe<TrCreditPhase>,
    computePh: TrComputePhase,
    action: Maybe<Cell<TrActionPhase>>,
    aborted: bool,
    bounce: Maybe<TrBouncePhase>,
    destroyed: bool
}
```

Represents ordinary transaction description.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L299" />

## `TransStorage`

```tolk
struct (0b0001) TransStorage {
    storagePh: TrStoragePhase
}
```

Represents storage transaction description.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L312" />

## `TransTickTock`

```tolk
@overflow1023_policy("suppress")
struct (0b001) TransTickTock {
    isTock: bool,
    storagePh: TrStoragePhase,
    computePh: TrComputePhase,
    action: Maybe<Cell<TrActionPhase>>,
    aborted: bool,
    destroyed: bool
}
```

Represents tick-tock transaction description.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L317" />

## `SplitMergeInfo`

```tolk
struct SplitMergeInfo {
    curShardPfxLen: uint8, // ## 6
    accSplitDepth: uint8,   // ## 6
    thisAddr: bits256,
    siblingAddr: bits256
}
```

Represents split-merge information.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L328" />

## `TransSplitPrepare`

```tolk
@overflow1023_policy("suppress")
struct (0b0100) TransSplitPrepare {
    splitInfo: SplitMergeInfo,
    storagePh: Maybe<TrStoragePhase>,
    computePh: TrComputePhase,
    action: Maybe<Cell<TrActionPhase>>,
    aborted: bool,
    destroyed: bool
}
```

Represents split prepare transaction description.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L336" />

## `TransSplitInstall`

```tolk
struct (0b0101) TransSplitInstall {
    splitInfo: SplitMergeInfo,
    prepareTransaction: Cell<Transaction>,
    installed: bool
}
```

Represents split install transaction description.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L347" />

## `TransMergePrepare`

```tolk
struct (0b0110) TransMergePrepare {
    splitInfo: SplitMergeInfo,
    storagePh: TrStoragePhase,
    aborted: bool
}
```

Represents merge prepare transaction description.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L354" />

## `TransMergeInstall`

```tolk
@overflow1023_policy("suppress")
struct (0b0111) TransMergeInstall {
    splitInfo: SplitMergeInfo,
    prepareTransaction: Cell<Transaction>,
    storagePh: Maybe<TrStoragePhase>,
    creditPh: Maybe<TrCreditPhase>,
    computePh: TrComputePhase,
    action: Maybe<Cell<TrActionPhase>>,
    aborted: bool,
    destroyed: bool
}
```

Represents merge install transaction description.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L361" />

## `varUintSize`

```tolk
fun varUintSize(value: int, maxBytes: int): int
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L385" />

## `VarUint7.packToBuilder`

```tolk
fun VarUint7.packToBuilder(self, mutate b: builder)
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L407" />

## `VarUint7.unpackFromSlice`

```tolk
fun VarUint7.unpackFromSlice(mutate s: slice): VarUint7
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L415" />

## `VarUint3.packToBuilder`

```tolk
fun VarUint3.packToBuilder(self, mutate b: builder)
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L422" />

## `VarUint3.unpackFromSlice`

```tolk
fun VarUint3.unpackFromSlice(mutate s: slice): VarUint3
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L430" />

## `StorageUsed`

```tolk
struct StorageUsed {
    cells: VarUint7,
    bits: VarUint7
}
```

Represents storage used information.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L436" />

## `StorageExtraNone`

```tolk
struct (0b000) StorageExtraNone
```

Represents storage extra information.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L442" />

## `StorageExtraDict`

```tolk
struct (0b001) StorageExtraDict {
    dictHash: uint256
}
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L443" />

## `StorageInfo`

```tolk
struct StorageInfo {
    /// Storage used.
    used: StorageUsed,
    /// Storage extra information.
    storageExtra: StorageExtraInfo,
    /// Last paid time.
    lastPaid: uint32,
    /// Due payment (optional).
    duePayment: Maybe<coins>
}
```

Represents storage information.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L451" />

## `AccountNone`

```tolk
struct (0b0) AccountNone
```

Represents account.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L463" />

## `AccountInfo`

```tolk
struct (0b1) AccountInfo {
    /// Account address.
    addr: address,
    /// Storage statistics.
    storageStat: StorageInfo,
    /// Account storage.
    storage: AccountStorage
}
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L464" />

## `AccountStateUninit`

```tolk
struct (0b00) AccountStateUninit
```

Account state in AccountStorage (block.tlb `AccountState`).

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L477" />

## `AccountStateActive`

```tolk
struct (0b1) AccountStateActive {
    stateInit: StateInit
}
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L478" />

## `AccountStateFrozen`

```tolk
struct (0b01) AccountStateFrozen {
    stateHash: uint256
}
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L481" />

## `AccountStorage`

```tolk
struct AccountStorage {
    /// Last transaction logical time.
    lastTransLt: uint64,
    /// Account balance.
    balance: CurrencyCollection,
    /// Account state.
    state: AccountState
}
```

Represents account storage.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L489" />

## `ShardAccount`

```tolk
struct ShardAccount {
    account: Cell<Account>,
    lastTransHash: uint256,
    lastTransLt: uint64,
}
```

Represents a shard account state.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/transaction.tolk#L499" />

