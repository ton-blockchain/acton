---
title: "vm"
description: "vm.tolk standard library file"
---

import { SourceCodeLink } from '@/components/SourceCodeLink';

Module for VM interaction.

This module provides low-level access to the TON Virtual Machine (TVM) state during test execution.
It allows reading and writing to control registers (C5, C7) and configuring execution parameters.

Examples:
```tolk
// Mock time
vm.setTime(1700000000);

// Register a mock library
vm.registerLibrary(myLibCell);

// Inspect actions manually
val rawActions = vm.getC5();
```

## Definitions

## `vm`

```tolk
struct vm
```

Holds the methods to change the VM state.

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/vm/vm.tolk#L20" />

## `vm.getC7`

```tolk
fun vm.getC7(): tuple
```

Read control register C7.

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/vm/vm.tolk#L23" />

## `vm.setC7`

```tolk
fun vm.setC7(c7: tuple): void
```

Write a new value to control register C7.

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/vm/vm.tolk#L27" />

## `vm.getC5`

```tolk
fun vm.getC5(): cell
```

Read control register C5.

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/vm/vm.tolk#L31" />

## `vm.setC5`

```tolk
fun vm.setC5(cell: cell): void
```

Write a new value to control register C5.

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/vm/vm.tolk#L35" />

## `vm.getConfigParam`

```tolk
fun vm.getConfigParam<T>(index: int): T
```

Generic helper to get a single config parameter.

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/vm/vm.tolk#L39" />

## `vm.setConfigParam`

```tolk
fun vm.setConfigParam<T>(value: T, index: int): void
```

Generic helper to overwrite a single config parameter.

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/vm/vm.tolk#L46" />

## `CONFIG_ROOT_INDEX`

```tolk
const CONFIG_ROOT_INDEX = 9
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/vm/vm.tolk#L54" />

## `vm.setConfigRootDict`

```tolk
fun vm.setConfigRootDict(root: dict): void
```

Set the blockchain configuration root dictionary.

This function allows you to override the configuration root dictionary (parameter 9) in the VM state.
The config root is a dictionary containing blockchain configuration parameters.
It writes C7 slot 9 only.

Note that this does not recompute unpacked config in C7 slot 14.
After [vm.setConfigRootDict](#vmsetconfigrootdict), [vm.getConfigUnpacked](#vmgetconfigunpacked) can still return stale values.

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/vm/vm.tolk#L64" />

## `CONFIG_UNPACKED_INDEX`

```tolk
const CONFIG_UNPACKED_INDEX = 14
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/vm/vm.tolk#L73" />

## `vm.setConfigUnpacked`

```tolk
fun vm.setConfigUnpacked(values: tuple): void
```

Set the unpacked configuration parameters tuple.

This function allows you to override the unpacked configuration parameters (parameter 14) in the VM state.
The unpacked config contains a tuple of configuration values extracted from the config dictionary.
It writes C7 slot 14 directly and does not validate tuple shape.

Use [vm.getConfigUnpacked](#vmgetconfigunpacked) as a template for expected tuple layout.
If this tuple is malformed (for example, too short), later operations that assume
specific indices can fail with runtime errors such as `Range check error`.

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/vm/vm.tolk#L84" />

## `vm.getConfigUnpacked`

```tolk
fun vm.getConfigUnpacked(): tuple
```

Get the unpacked configuration parameters tuple.

Retrieves the unpacked configuration parameters (parameter 14) from the current VM state.
Returns a tuple of configuration values from C7 slot 14 as-is.
This value is independent from C7 slot 9 set via [vm.setConfigRootDict](#vmsetconfigrootdict).

Example:
```tolk
val unpacked = vm.getConfigUnpacked();
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/vm/vm.tolk#L103" />

## `vm.setTime`

```tolk
fun vm.setTime(now: int): void
```

Override `blockchain.now()` for deterministic testing.

Writes C7 parameter tuple slot 3 and affects `blockchain.now()` reads.

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/vm/vm.tolk#L113" />

## `vm.setBlockLogicalTime`

```tolk
fun vm.setBlockLogicalTime(blockLogicalTime: int): void
```

Override the **block** logical time.

Writes C7 parameter tuple slot 4.
Does not change slots 3 (`now`) or 5 (`logical time`).

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/vm/vm.tolk#L121" />

## `vm.setLogicalTime`

```tolk
fun vm.setLogicalTime(logicalTime: int): void
```

Override the **transaction** logical time.

Writes C7 parameter tuple slot 5.
Does not change slots 3 (`now`) or 4 (`block logical time`).

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/vm/vm.tolk#L129" />

## `vm.setOriginalBalance`

```tolk
fun vm.setOriginalBalance(balance: coins, extraCurrencies: dict = null): void
```

Override the original balance of the contract.

Writes C7 parameter tuple slot 7 as `[balance, extraCurrencies]`.

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/vm/vm.tolk#L136" />

## `vm.registerLibrary`

```tolk
fun vm.registerLibrary(cell: cell): void
```

Register a new library for current test.

Re-registering the same library cell is idempotent and does not change C5/C7.

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/vm/vm.tolk#L143" />

## `vm.convertAddress`

```tolk
fun vm.convertAddress(addr: string): address
```

Convert a string representation of address to an internal [address](../../../tolk_standard_library/common/#address) value.

Accepted forms include raw addresses (`0:...`) and user-friendly TON addresses
(base64/base64url with flags).
The returned value is normalized as an internal address; rendered formatting may
differ from input flags, while representing the same account.
Invalid input fails with an error like `Failed to convert address from <input>`.

Example:
```tolk
val address = vm.convertAddress("kQCPFJYFqhowz0BRAfqNPdLOC1_Rlupki_0BezWU6rHk5lbt");
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/vm/vm.tolk#L159" />

## `vm.cellFromHex`

```tolk
fun vm.cellFromHex(cellHex: string): cell
```

Decode a root cell from hex-encoded BOC bytes.

`cellHex` must be a valid BOC serialized as hex.
Invalid or malformed input (including non-BOC payloads) fails with an error like
`Failed to decode cell hex <input>`.

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/vm/vm.tolk#L169" />

## `vm.parseAddress`

```tolk
fun vm.parseAddress(s: string): address
```

Parse a string representation of address into an internal [address](../../../tolk_standard_library/common/#address) value.

Accepted forms include raw addresses (`0:...`) and user-friendly TON addresses
(base64/base64url with flags).
Invalid input fails with an error.

Example:
```tolk
val addr = vm.parseAddress("kQCPFJYFqhowz0BRAfqNPdLOC1_Rlupki_0BezWU6rHk5lbt");
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/vm/vm.tolk#L183" />

## `vm.parseInt`

```tolk
fun vm.parseInt(x: string): int
```

Parse a decimal string into an [int](../../../tolk_standard_library/common/#int) value.

Accepts optional leading `-` sign followed by decimal digits.
Invalid input (non-numeric characters, empty string) fails with an error.

Example:
```tolk
val n = vm.parseInt("12345");
val negative = vm.parseInt("-42");
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/vm/vm.tolk#L198" />

