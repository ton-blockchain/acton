---
title: "expect"
description: "expect.tolk standard library file"
---

import { SourceCodeLink } from '@/components/SourceCodeLink';

{/* @generated by `acton docgen`. Do not edit directly. */}
{/* Source: `lib/testing/expect.tolk` */}

Module for high-level assertion API.

This module provides a fluent "expect" style API for writing test assertions.
It wraps the lower-level `Assert` functions and provides a more readable syntax.
It supports a wide range of types including integers, booleans, slices, addresses, tuples, maps, and optionals.

Examples:
```tolk
// Simple value check
expect(total).toEqual(100);

// Comparison
expect(balance).toBeGreater(ton("1.0"));

// Approximate check (useful for gas calculations)
expect(gasUsed).toBeApproxEqRel(expectedGas, 10); // within 10%

// Map check
expect(balances).toContainKey(userAddress);
expect(balances.mustGet(userAddress)).toEqual(ton("50"));
```

## Definitions

## `Expectation`

```tolk
struct Expectation<T> {
    /// The value to be expected.
    private value: T
    /// The type of the value to be expected.
    private valueType: int
    /// The location of the expectation to be printed in the error message.
    /// This field is set by the test framework and should not be set manually.
    private location: string
}
```

Internal struct to represent an expectation for a value.

Don't use this struct directly. Use the `expect` function instead.

Example:
```tolk
expect(10).toEqual(10);
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L39" />

## `Expectation<T>.create`

```tolk
fun Expectation<T>.create(value: T, valueType: int, location: string): Expectation<T>
```

Internal function to create a new expectation for the given value.

Don't use this function directly. Use the `expect` function instead.

Example:
```tolk
expect(10).toEqual(10);
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L57" />

## `Expectation<T>.fail`

```tolk
fun Expectation<T>.fail(self, message: string): never
```

Fails this expectation at its location.
A convenient wrapper to be used in `Expectation<T>.xxx` methods instead of `Assert.fail`.

Example:
```tolk
fun Expectation<int>.toBeTen(self) {
    if (self.value != 10) {
        self.fail("integer is not 10");
    }
}
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L72" />

## `Expectation<T>.toEqual`

```tolk
fun Expectation<T>.toEqual<U>(self, expected: U): void
```

Asserts that actual value is equal to expected value.

Example:
```tolk
expect(10).toEqual(10);
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L82" />

## `Expectation<T>.toEqualDecimal`

```tolk
fun Expectation<T>.toEqualDecimal(self, expected: T, decimals: int): void
```

Asserts that actual value is equal to expected value as decimal with given precision.

Comparison itself is exact (no rounding is applied to actual or expected values).
`decimals` controls only failure-message formatting:
decimal point insertion, trailing-zero trimming, and normalized integer-style output (`.0`).

Example:
```tolk
expect(ton("1.5")).toEqualDecimal(ton("1.5"), 9);
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L96" />

## `Expectation<T>.toNotEqual`

```tolk
fun Expectation<T>.toNotEqual<U>(self, expected: U): void
```

Asserts that actual value is not equal to expected value.

Example:
```tolk
expect(10).toNotEqual(20);
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L106" />

## `Expectation<T>.toBeLess`

```tolk
fun Expectation<T>.toBeLess<U>(self, expected: U): void
```

Asserts that actual value is less than expected value.

Example:
```tolk
expect(5).toBeLess(10);
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L116" />

## `Expectation<T>.toBeGreater`

```tolk
fun Expectation<T>.toBeGreater<U>(self, expected: U): void
```

Asserts that actual value is greater than expected value.

Example:
```tolk
expect(10).toBeGreater(5);
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L132" />

## `Expectation<T>.toBeLessOrEqual`

```tolk
fun Expectation<T>.toBeLessOrEqual<U>(self, expected: U): void
```

Asserts that actual value is less than or equal to expected value.

Example:
```tolk
expect(5).toBeLessOrEqual(10);
expect(10).toBeLessOrEqual(10);
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L149" />

## `Expectation<T>.toBeGreaterOrEqual`

```tolk
fun Expectation<T>.toBeGreaterOrEqual<U>(self, expected: U): void
```

Asserts that actual value is greater than or equal to expected value.

Example:
```tolk
expect(10).toBeGreaterOrEqual(5);
expect(10).toBeGreaterOrEqual(10);
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L166" />

## `Expectation<int>.toBeApproxEqAbs`

```tolk
fun Expectation<int>.toBeApproxEqAbs(self, expected: int, maxDelta: int): void
```

Asserts that actual value is approximately equal to expected value using absolute difference.

Example:
```tolk
expect(10).toBeApproxEqAbs(15, 10); // ok, difference is 5, which is less than maximum allowed difference of 10
expect(10).toBeApproxEqAbs(20, 5); // error, difference is 10, which is greater than maximum allowed difference of 5
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L183" />

## `Expectation<int>.toBeApproxEqRel`

```tolk
fun Expectation<int>.toBeApproxEqRel(self, expected: int, maxDelta: int): void
```

Asserts that actual value is approximately equal to expected value using relative percentage difference.

Relative delta is computed as:
`floor(abs(actual - expected) * 100 / abs(actual))`.
The assertion passes when this computed percentage is less than or equal to `maxDelta`.

Zero branch:
if `actual == 0`, only `expected == 0` passes; any non-zero `expected` is treated as infinite delta and fails.

Example:
```tolk
expect(1000).toBeApproxEqRel(1099, 9);
expect(0).toBeApproxEqRel(0, 0);
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L212" />

## `Expectation<T?>.toBeNull`

```tolk
fun Expectation<T?>.toBeNull(self): void
```

Asserts that actual value is null.

Example:
```tolk
expect(null).toBeNull();
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L249" />

## `Expectation<T?>.toBeNotNull`

```tolk
fun Expectation<T?>.toBeNotNull(self): void
```

Asserts that actual value is not null.

Example:
```tolk
val someValue: int? = cond ? 10 : null;
expect(someValue).toBeNotNull();
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L262" />

## `Expectation<slice>.toBeNonEmpty`

```tolk
fun Expectation<slice>.toBeNonEmpty(self): void
```

Asserts that actual value is not empty (contains some bits of refs).

Example:
```tolk
expect(someSlice).toBeNonEmpty();
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L274" />

## `Expectation<slice>.toBeEmpty`

```tolk
fun Expectation<slice>.toBeEmpty(self): void
```

Asserts that actual value is empty (contains 0 bits and 0 refs).

Example:
```tolk
expect(someSlice).toBeEmpty();
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L286" />

## `Expectation<int>.toBeNaN`

```tolk
fun Expectation<int>.toBeNaN(self): void
```

Asserts that actual value is NaN.

Example:
```tolk
expect(nan()).toBeNaN();
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L298" />

## `Expectation<int>.toBeNonNaN`

```tolk
fun Expectation<int>.toBeNonNaN(self): void
```

Asserts that actual value is not NaN.

Example:
```tolk
expect(0).toBeNonNaN();
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L308" />

## `Expectation<bool>.toBeTrue`

```tolk
fun Expectation<bool>.toBeTrue(self): void
```

Asserts that actual value is true.

Example:
```tolk
expect(true).toBeTrue();
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L318" />

## `Expectation<bool>.toBeFalse`

```tolk
fun Expectation<bool>.toBeFalse(self): void
```

Asserts that actual value is false.

Example:
```tolk
expect(false).toBeFalse();
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L328" />

## `Expectation<any_address>.toBeInternalAddress`

```tolk
fun Expectation<any_address>.toBeInternalAddress(self): void
```

Asserts that actual address is internal.

Example:
```tolk
expect(addr).toBeInternalAddress();
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L338" />

## `Expectation<any_address>.toBeNoneAddress`

```tolk
fun Expectation<any_address>.toBeNoneAddress(self): void
```

Asserts that actual address is none.

Example:
```tolk
expect(addr).toBeNoneAddress();
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L350" />

## `Expectation<any_address>.toBeExternalAddress`

```tolk
fun Expectation<any_address>.toBeExternalAddress(self): void
```

Asserts that actual address is external.

Example:
```tolk
expect(addr).toBeExternalAddress();
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L362" />

## `Expectation<array<E>>.toContain`

```tolk
fun Expectation<array<E>>.toContain<U>(self, expected: U): void
```

Asserts that an array contains the expected value.

Example:
```tolk
val arr = [1, 2, 3];
expect(arr).toContain(2);
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L375" />

## `Expectation<array<E>>.toNotContain`

```tolk
fun Expectation<array<E>>.toNotContain<U>(self, expected: U): void
```

Asserts that an array does not contain the expected value.

Example:
```tolk
val arr = [1, 2, 3];
expect(arr).toNotContain(4);
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L394" />

## `Expectation<array<E>>.toBeEmpty`

```tolk
fun Expectation<array<E>>.toBeEmpty(self): void
```

Asserts that an array is empty.

Example:
```tolk
val arr = array<int> [];
expect(arr).toBeEmpty();
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L412" />

## `Expectation<BigArray<E>>.toBeEmpty`

```tolk
fun Expectation<BigArray<E>>.toBeEmpty(self): void
```

Asserts that an array is empty.

Example:
```tolk
val txs = net.send(deployer.address, msg);
expect(txs).toBeEmpty();
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L425" />

## `Expectation<array<E>>.toBeNonEmpty`

```tolk
fun Expectation<array<E>>.toBeNonEmpty(self): void
```

Asserts that an array is not empty.

Example:
```tolk
val arr = [1, 2, 3];
expect(arr).toBeNonEmpty();
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L438" />

## `Expectation<BigArray<E>>.toBeNonEmpty`

```tolk
fun Expectation<BigArray<E>>.toBeNonEmpty(self): void
```

Asserts that an array is not empty.

Example:
```tolk
val txs = net.send(deployer.address, msg);
expect(txs).toBeNonEmpty();
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L451" />

## `Expectation<array<E>>.toHaveLength`

```tolk
fun Expectation<array<E>>.toHaveLength(self, length: int): void
```

Asserts that an array has the expected length.

Example:
```tolk
val arr = [1, 2, 3];
expect(arr).toHaveLength(3);
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L464" />

## `Expectation<BigArray<E>>.toHaveLength`

```tolk
fun Expectation<BigArray<E>>.toHaveLength(self, length: int): void
```

Asserts that an array has the expected length.

Example:
```tolk
val txs = net.send(deployer.address, msg);
expect(txs).toHaveLength(3);
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L480" />

## `Expectation<lisp_list<E>>.toContain`

```tolk
fun Expectation<lisp_list<E>>.toContain<U>(self, expected: U): void
```

Asserts that a lisp list contains the expected value.

Example:
```tolk
val list = lisp_list<int> [1, 2, 3];
expect(list).toContain(2);
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L496" />

## `Expectation<lisp_list<E>>.toNotContain`

```tolk
fun Expectation<lisp_list<E>>.toNotContain<U>(self, expected: U): void
```

Asserts that a lisp list does not contain the expected value.

Example:
```tolk
val list = lisp_list<int> [1, 2, 3];
expect(list).toNotContain(4);
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L520" />

## `Expectation<lisp_list<E>>.toBeEmpty`

```tolk
fun Expectation<lisp_list<E>>.toBeEmpty(self): void
```

Asserts that a lisp list is empty.

Example:
```tolk
val list = lisp_list<int> [];
expect(list).toBeEmpty();
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L543" />

## `Expectation<lisp_list<E>>.toBeNonEmpty`

```tolk
fun Expectation<lisp_list<E>>.toBeNonEmpty(self): void
```

Asserts that a lisp list is not empty.

Example:
```tolk
val list = lisp_list<int> [1, 2, 3];
expect(list).toBeNonEmpty();
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L556" />

## `Expectation<lisp_list<E>>.toHaveLength`

```tolk
fun Expectation<lisp_list<E>>.toHaveLength(self, length: int): void
```

Asserts that a lisp list has the expected length.

Example:
```tolk
val list = lisp_list<int> [1, 2, 3];
expect(list).toHaveLength(3);
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L569" />

## `Expectation<map<K, V>>.toContainKey`

```tolk
fun Expectation<map<K, V>>.toContainKey(self, key: K): void
```

Asserts that a map contains the expected key.

Example:
```tolk
var map = map<int32, int32> [];
map.set(1, 2);
map.set(2, 3);
expect(map).toContainKey(2);
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L587" />

## `Expectation<map<K, V>>.toNotContainKey`

```tolk
fun Expectation<map<K, V>>.toNotContainKey(self, key: K): void
```

Asserts that actual value represeneted as map does NOT contain the expected key.

Example:
```tolk
var map = map<int32, int32> [];
map.set(1, 2);
map.set(2, 3);
expect(map).toNotContainKey(4);
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L608" />

## `Expectation<map<K, V>>.toContainValue`

```tolk
fun Expectation<map<K, V>>.toContainValue(self, value: V): void
```

Asserts that actual value represeneted as map contains the expected value.

Example:
```tolk
var map = map<int32, int32> [];
map.set(1, 2);
map.set(2, 3);
expect(map).toContainValue(2);
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L629" />

## `Expectation<map<K, V>>.toNotContainValue`

```tolk
fun Expectation<map<K, V>>.toNotContainValue(self, value: V): void
```

Asserts that actual value represeneted as map does NOT contain the expected value.

Example:
```tolk
var map = map<int32, int32> [];
map.set(1, 2);
map.set(2, 3);
expect(map).toNotContainValue(4);
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L656" />

## `Expectation<map<K, V>>.toBeEmpty`

```tolk
fun Expectation<map<K, V>>.toBeEmpty(self): void
```

Asserts that actual value represeneted as map is empty.

Example:
```tolk
var map = map<int32, int32> [];
expect(map).toBeEmpty();
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L679" />

## `Expectation<map<K, V>>.toBeNonEmpty`

```tolk
fun Expectation<map<K, V>>.toBeNonEmpty(self): void
```

Asserts that actual value represeneted as map is not empty.

Example:
```tolk
var map = map<int32, int32> [];
map.set(1, 2);
map.set(2, 3);
expect(map).toBeNonEmpty();
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L694" />

## `Expectation<map<K, V>>.toHaveLength`

```tolk
fun Expectation<map<K, V>>.toHaveLength(self, length: int): void
```

Asserts that actual value represeneted as map has the expected length.

Example:
```tolk
var map = map<int32, int32> [];
map.set(1, 2);
map.set(2, 3);
expect(map).toHaveLength(2);
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L709" />

## `expect`

```tolk
fun expect<T>(value: T, loc: string = reflect.sourceLocationAsString()): Expectation<T>
```

Creates a new expectation for the given value.

After creation, the expectation can be used to assert the value.

For example:
```tolk
expect(10).toEqual(10);
```

Optional location can be provided to specify the location of the expectation to
be printed in the error message. You likely don't want to set this manually,
as the location will be automatically set by the test framework.

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L732" />

## `expectToEndWithExitCode`

```tolk
fun expectToEndWithExitCode(code: int): void
```

Sets the expected exit code for the test.

Example:
```tolk
expectToEndWithExitCode(10);
throw 10; // Test will end with exit code 10 and be considered as passed
```

Alternatively, you can use `@test.fail_with(10)` annotation to set
the expected exit code for the whole test.

```tolk
@test.fail_with(10)
get fun `test fail with`() {
    throw 10; // ok
}
```

This function is more flexible and can be used conditionally in the test.

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/testing/expect.tolk#L759" />

