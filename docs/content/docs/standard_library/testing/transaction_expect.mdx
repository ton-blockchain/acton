---
title: "transaction_expect"
description: "transaction_expect.tolk standard library file"
---

import { SourceCodeLink } from '@/components/SourceCodeLink';

Module for asserting properties of transactions.

This module extends the `Expectation` class specifically for `SendResultList` and `SendResult`.
It allows verifying the outcome of network operations, such as checking if a transaction succeeded,
if a contract was deployed, or if specific messages were emitted.

Matchers accept `SearchParams`; all non-null fields are used as filters.
Generic matcher variants (`<Msg>`) additionally inject opcode filtering from `Msg` declared prefix (if present).

Examples:
```tolk
val results = net.send(wallet.address, msg);

// Verify general success
expect(results).toHaveSuccessfulTx({
    from: wallet.address,
    to: contract.address
});

// Verify deployment
expect(results).toHaveSuccessfulDeploy({
    to: contract.address
});

// Verify failure case
val failResults = net.send(wallet.address, invalidMsg);
expect(failResults).toHaveFailedTx({
    to: contract.address,
    exitCode: 102 // specific error code
});
```

## Definitions

## `Expectation<SendResultList>.toHaveSuccessfulDeploy`

```tolk
fun Expectation<SendResultList>.toHaveSuccessfulDeploy<Msg = unknown>(self, params: SearchParams = {})
```

Expect the transaction list to have a successful deploy transaction.

Transaction is considered as "deploy" if previous status was "NonExisting" and end status was "Active".
If generic `Msg` is provided and has declared prefix, matcher also filters by `params.opcode`.

Example:

```tolk
expect(txs).toHaveSuccessfulDeploy({
    to: counter.address,
});
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/testing/transaction_expect.tolk#L52" />

## `Expectation<SendResultList>.toHaveSuccessfulTx`

```tolk
fun Expectation<SendResultList>.toHaveSuccessfulTx<Msg = unknown>(self, params: SearchParams = {})
```

Expect the transaction list to have a successful transaction.

Matcher sets `params.success = true`.
Runtime "success" means both compute phase and action phase are successful.

By default matcher also requires `params.exitCode = 0` when `exitCode` is not provided.
This default can be overridden with explicit `params.exitCode`.

If generic `Msg` is provided and has declared prefix, matcher also filters by `params.opcode`.

Example:
```tolk
expect(txs).toHaveSuccessfulTx({
    from: deployer.address,
    to: counter.address,
});
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/testing/transaction_expect.tolk#L89" />

## `Expectation<SendResultList>.toHaveTx`

```tolk
fun Expectation<SendResultList>.toHaveTx<Msg = unknown>(self, params: SearchParams = {})
```

Expect the transaction list to have a transaction with specified parameters.

All non-null `SearchParams` fields are applied as filters.

If generic `Msg` is provided and has declared prefix, matcher also filters by `params.opcode`.

Example:
```tolk
expect(txs).toHaveTx({
    from: deployer.address,
    to: counter.address,
    actionExitCode: 37,
});
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/testing/transaction_expect.tolk#L125" />

## `Expectation<SendResultList>.toNotHaveTx`

```tolk
fun Expectation<SendResultList>.toNotHaveTx<Msg = unknown>(self, params: SearchParams = {})
```

Expect the transaction list to NOT have a transaction with specified parameters.

Example:
```tolk
expect(txs).toNotHaveTx({
    from: deployer.address,
    to: counter.address,
});
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/testing/transaction_expect.tolk#L152" />

## `Expectation<SendResultList>.toHaveBouncedTx`

```tolk
fun Expectation<SendResultList>.toHaveBouncedTx<Msg = unknown>(self, params: SearchParams = {})
```

Expect the transaction list to have a **bounced** transaction.

Example:
```tolk
expect(txs).toHaveBouncedTx({
    from: deployer.address,
    to: counter.address,
});
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/testing/transaction_expect.tolk#L179" />

## `Expectation<SendResultList>.toHaveFailedTx`

```tolk
fun Expectation<SendResultList>.toHaveFailedTx<Msg = unknown>(self, params: SearchParams = {})
```

Expect the transaction list to have a **failed** transaction.

This matcher sets `params.success = false`.
`params.exitCode` is required and must be non-zero.
If `exitCode` is null or `0`, matcher fails immediately before transaction search.

Any additional non-null `SearchParams` fields further narrow the search.
If generic `Msg` is provided and has declared prefix, matcher also filters by `params.opcode`.

Example:
```tolk
expect(txs).toHaveFailedTx({
    from: deployer.address,
    to: counter.address,
    exitCode: 123,
});
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/testing/transaction_expect.tolk#L215" />

## `Expectation<SendResultList>.toHaveAllSuccessfulTxs`

```tolk
fun Expectation<SendResultList>.toHaveAllSuccessfulTxs(self)
```

Expect all transactions in the list to be successful.

Example:
```tolk
val results = net.send(wallet.address, msg);
expect(results).toHaveAllSuccessfulTxs();
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/testing/transaction_expect.tolk#L247" />

## `Expectation<SendResultList>.toEmitExternalMessage`

```tolk
fun Expectation<SendResultList>.toEmitExternalMessage<Msg>(self)
```

Expect the transaction to emit an external message of given type.

Example:
```tolk
expect(txs).toEmitExternalMessage<IncreaseCounter>();
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/testing/transaction_expect.tolk#L271" />

## `Expectation<SendResult>.toConsumeLessThan`

```tolk
fun Expectation<SendResult>.toConsumeLessThan(self, amount: int)
```

Expect the transaction to consume less than the expected amount of gas.

Example:
```tolk
val result = net.send(deployer.address, message);
expect(result.at(0)).toConsumeLessThan(1000);
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/testing/transaction_expect.tolk#L307" />

## `T.getDeclaredPackPrefix`

```tolk
fun T.getDeclaredPackPrefix()
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/testing/transaction_expect.tolk#L317" />

