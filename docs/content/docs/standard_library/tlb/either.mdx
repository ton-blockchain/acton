---
title: "either"
description: "either.tolk standard library file"
---

import { SourceCodeLink } from '@/components/SourceCodeLink';

Tagged union that stores either `X` (left) or `Y` (right).

Alias semantics:
- `Either<X, Y>` is exactly `EitherLeft<X> | EitherRight<Y>`.

Encoding semantics:
- Left branch starts with tag bit `0` (`0b0`).
- Right branch starts with tag bit `1` (`0b1`).

Typical consumption pattern:
```tolk
fun branchScore(choice: Either<uint32, uint32>): int {
    return match (choice) {
        EitherLeft => 1000 + choice.value,
        EitherRight => 2000 + choice.value,
    };
}
```

## Definitions

## `Either`

```tolk
type Either<X, Y> = EitherLeft<X> | EitherRight<Y>
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/tlb/either.tolk#L19" />

## `Either<X, Y>.left`

```tolk
@inline
fun Either<X, Y>.left(value: X): EitherLeft<X>
```

Left constructor.
Encodes with tag bit `0` followed by `value`.

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/tlb/either.tolk#L23" />

## `Either<X, Y>.right`

```tolk
@inline
fun Either<X, Y>.right(value: Y): EitherRight<Y>
```

Right constructor.
Encodes with tag bit `1` followed by `value`.

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/tlb/either.tolk#L30" />

## `EitherLeft`

```tolk
struct (0b0) EitherLeft<X> {
    value: X
}
```

Left variant with explicit TLB tag `0b0`.

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/tlb/either.tolk#L36" />

## `EitherRight`

```tolk
struct (0b1) EitherRight<X> {
    value: X
}
```

Right variant with explicit TLB tag `0b1`.

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/tlb/either.tolk#L41" />

