---
title: "tracing"
description: "tracing.tolk standard library file"
---

import { SourceCodeLink } from '@/components/SourceCodeLink';

## Definitions

## `tracing`

```tolk
struct tracing
```

Utilities for naming and organizing traces produced in tests.

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/emulation/tracing.tolk#L5" />

## `tracing.save`

```tolk
fun tracing.save(txs: SendResultList, name: string): void
```

Save a custom name for a trace chain produced by [net.send](../network/#netsend) or [net.sendIter](../network/#netsenditer).

The custom name is used in the Test UI and gas/fee profiling reports.

Example:
```tolk
val txs = net.send(deployer.address, msg);
tracing.save(txs, "deploy-counter");
expect(txs).toHaveAllSuccessfulTxs();
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/lib/emulation/tracing.tolk#L17" />

