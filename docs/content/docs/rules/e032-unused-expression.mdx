---
title: "E032: unused-expression"
description: "Warns when an expression statement computes a value that is immediately discarded."
---

import { SourceCodeLink } from '@/components/SourceCodeLink';

{/* @generated by `acton docgen`. Do not edit directly. */}
{/* Source: `crates/tolk-linter/src/rules/ast/unused_expression.rs` */}

## Metadata

- `Code`: `E032`
- `Rule`: `unused-expression`
- `Status`: Stable since `v0.0.1`
- `Quick fix`: not available

### What it does
Warns when an expression statement computes a value that is immediately discarded.

### Why is this bad?
Such expressions usually have no effect and often indicate a typo, such as using `!=`
instead of `=` or `+=`.

### Example
```tolk twoslash
fun main(a: int, b: int) {
    a != b;
//  ^^^^^^^ E032: unused expression has no effect
}
```

Use instead:
```tolk
fun main(mutate a: int, b: int) {
    a -= b;
}
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/crates/tolk-linter/src/rules/ast/unused_expression.rs#L29" />
