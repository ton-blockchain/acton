---
title: "E013: mutable-parameter-can-be-immutable"
description: "Checks for parameters that are declared as mutable (`mutate`) but are never mutated."
---

import { SourceCodeLink } from '@/components/SourceCodeLink';

## Metadata

- `Code`: `E013`
- `Rule`: `mutable-parameter-can-be-immutable`
- `Status`: Stable since `v0.0.1`
- `Quick fix`: always available

### What it does
Checks for parameters that are declared as mutable (`mutate`) but are never mutated.

### Why is this bad?
Using immutable parameters makes the function contract clearer and reduces accidental mutations.

### Example
```tolk
fun increment(mutate value: int): int {
    return value + 1;
}
```

Use instead:
```tolk
fun increment(value: int): int {
    return value + 1;
}
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/crates/tolk-linter/src/rules/ast/mutable_parameter_can_be_immutable.rs#L31" />
