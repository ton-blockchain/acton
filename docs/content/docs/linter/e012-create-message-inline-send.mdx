---
title: "E012: create-message-inline-send"
description: "Checks for inline sending of newly created messages: `createMessage(...).send(...)`."
---

import { SourceCodeLink } from '@/components/SourceCodeLink';

## Metadata

- `Code`: `E012`
- `Rule`: `create-message-inline-send`
- `Status`: Stable since `v0.0.1`
- `Quick fix`: not available

### What it does
Checks for inline sending of newly created messages: `createMessage(...).send(...)`.

### Why is this bad?
Creating and sending message in one expression hides message intent.
Prefer giving message a name before sending.

### Example
```tolk
createMessage({ ... }).send(SEND_MODE_REGULAR);
```

Use instead:
```tolk
val deployMessage = createMessage({ ... });
deployMessage.send(SEND_MODE_REGULAR);
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/crates/tolk-linter/src/rules/ast/message_entity_naming.rs#L60" />
