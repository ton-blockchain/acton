---
title: "How to manage localnet nodes and validators"
sidebarTitle: "Manage nodes and validators"
description: "Add nodes, control validation, and inspect election membership on a localnet"
---

import {Tabs, Tab} from "fumadocs-ui/components/tabs";

<AvailabilityBadge since="Acton 1.2" />

Use additional localnet nodes to test synchronization, validator participation, and consensus behavior.

<Callout type="caution">
  Removing a node deletes its localnet state. Removing an elected validator can disrupt consensus. Save a [snapshot](/docs/environments/how-to/snapshots) before changing topology, and wait for elected duties to end before removal.
</Callout>

## Prerequisites

- A running [localnet](/docs/environments/how-to/create-network).
- Docker memory and disk allocations sufficient for the additional nodes, as described in the [localnet requirements](/docs/environments/localnet/overview#requirements-and-startup).

## Manage participation

<Tabs items={["UI", "Localnet CLI"]}>
<Tab value="UI">

1. Open <kbd>Network</kbd> → <kbd>Nodes</kbd> and select <kbd>Add node</kbd>.
1. Enter a name and choose whether the new node participates as a validator. Confirm creation.
1. Inspect synchronization and readiness before relying on the node.
1. Use the node's validation controls to enter or leave validation. Inspect <kbd>Network</kbd> → <kbd>Validators</kbd> for current and next elected sets.

Node start/stop controls preserve its state. Removing an additional node deletes its state. The genesis validator cannot be removed through these controls.

</Tab>
<Tab value="Localnet CLI">

```bash
acton localnet node dev add validator-b --validator
acton localnet status dev --json
```

Without [`--validator`](/docs/commands/localnet#acton-localnet-node), the added node is a full node that does not participate in elections. Replace `<NODE_ID>` with the ID from the result or status. Enable or disable future participation:

```bash
acton localnet node dev enter-validation '<NODE_ID>'
acton localnet node dev leave-validation '<NODE_ID>'
```

After the node leaves both the current and next elected sets, remove it:

```bash
acton localnet node dev remove '<NODE_ID>'
```

</Tab>
</Tabs>

## Verify election membership

Entering validation enables election participation; it does not immediately elect the node. Leaving validation still requires serving its existing elected duties. Check the current and next sets before removal.
