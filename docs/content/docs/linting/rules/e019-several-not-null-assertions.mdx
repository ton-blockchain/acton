---
title: "E019: several-not-null-assertions"
description: "Detects repeated not-null assertions like `foo!!`."
---

import { SourceCodeLink } from '@/components/SourceCodeLink';

## Metadata

- `Code`: `E019`
- `Rule`: `several-not-null-assertions`
- `Status`: Stable since `v0.0.1`
- `Quick fix`: always available

### What it does
Detects repeated not-null assertions like `foo!!`.

### Why is this bad?
Repeating `!` is redundant: after the first not-null assertion the value is already non-null.

### Example
```tolk
val x = foo!!;
```

Use instead:
```tolk
val x = foo!;
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/crates/tolk-linter/src/rules/ast/several_not_null_assertions.rs#L25" />
