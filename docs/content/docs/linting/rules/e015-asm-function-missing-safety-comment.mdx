---
title: "E015: asm-function-missing-safety-comment"
description: "Requires documenting safety requirements for every declaration with an `asm` body."
---

import { SourceCodeLink } from '@/components/SourceCodeLink';

## Metadata

- `Code`: `E015`
- `Rule`: `asm-function-missing-safety-comment`
- `Status`: Stable since `v0.0.1`
- `Quick fix`: not available

### What it does
Requires documenting safety requirements for every declaration with an `asm` body.

### Why is this bad?
`asm` bypasses high-level safety guarantees. Without an explicit safety note,
it is hard to review assumptions and soundness constraints.

### Example
```tolk
fun lowLevelLoad(x: slice): int asm "LDI";
```

Use instead:
```tolk
// SAFETY: `x` always contains at least 32 bits.
fun lowLevelLoad(x: slice): int asm "LDI";
```

<SourceCodeLink href="https://github.com/ton-blockchain/acton/blob/master/crates/tolk-linter/src/rules/ast/asm_function_missing_safety_comment.rs#L27" />
