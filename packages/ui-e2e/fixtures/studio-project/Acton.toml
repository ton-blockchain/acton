[package]
name = "Studio TON Connect visual fixture"
description = "Deterministic project for Studio TON Connect browser tests"
version = "0.1.0"
license = "MIT"

[localnet]
accounts = ["visual-wallet"]
