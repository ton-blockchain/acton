; Editor text objects for Fift.

[
  (proc_definition)
  (proc_inline_definition)
  (proc_ref_definition)
  (method_definition)
] @function.around

(program) @class.around
((comment)+) @comment.around
