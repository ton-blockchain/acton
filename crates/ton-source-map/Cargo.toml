[package]
name = "ton-source-map"
version = "0.1.0"
edition = "2024"

[dependencies]
tycho-types = { version = "0.3.0", features = ["bigint"] }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.145"
