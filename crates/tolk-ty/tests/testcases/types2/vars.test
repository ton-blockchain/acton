========================================================================
Tuple variable declaration
========================================================================
fun main() {
    val [a] = [1];
    //!  ^ int
}
------------------------------------------------------------------------
ok

========================================================================
Tuple variables declaration
========================================================================
fun main() {
    val [a,
    //!  ^ int
         b,
    //!  ^ bool
         c] = [1, true, ""];
    //!  ^ string
}
------------------------------------------------------------------------
type inference error at offset 77: expected string, got slice

========================================================================
Tensor variables declaration with function call init
========================================================================
fun getData(): (int, slice) {}

fun main() {
    val (a,
    //!  ^ int
         b) = getData();
    //!  ^ slice
}
------------------------------------------------------------------------
type inference error at offset 54: expected int, got unknown
type inference error at offset 81: expected slice, got unknown

========================================================================
Tuple variable declaration with function call init
========================================================================
fun foo(): [int, slice, bool] {}

fun main() {
    val [
        first,
//!     ^ int
        second,
//!     ^ slice
        third,
//!     ^ bool
    ] = foo();
}
------------------------------------------------------------------------
ok
