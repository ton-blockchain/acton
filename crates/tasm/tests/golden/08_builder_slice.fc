#include "stdlib.fc";
#pragma version >=0.4.0;

() recv_internal(int msg_value, cell in_msg_full, slice in_msg_body) impure {
    var (v3, v4) = load_uint(begin_parse(begin_cell()
        .store_uint(msg_value, 16)
        .end_cell()), 16);
}

