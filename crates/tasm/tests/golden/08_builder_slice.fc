#include "stdlib.fc";
#pragma version >=0.4.0;

() recv_internal(int msg_value, cell in_msg_full, slice in_msg_body) impure {
    var v2 = begin_parse(end_cell(begin_cell().store_uint(msg_value, 16)));
    var (v3, v4) = load_uint(v2, 16);
}

