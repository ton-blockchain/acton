#include "stdlib.fc";
#pragma version >=0.4.0;

() recv_internal(int msg_value, cell in_msg_full, slice in_msg_body) impure {
    var v0 = begin_parse(get_data());
    var v2 = v0~load_uint(32);
    end_parse(v0);
    if (slice_empty?(in_msg_body)) {
        var v4 = v2 + 1;
        set_data(end_cell(begin_cell().store_uint(v4, 32)));
        return ();
    }
    var (v7, v8) = load_uint(in_msg_body, 32);
    if (v8 == 1) {
        var v10 = v2 + 1;
        set_data(end_cell(begin_cell().store_uint(v10, 32)));
        return ();
    }
    if (v8 == 2) {
        var v14 = v2 - 1;
        set_data(end_cell(begin_cell().store_uint(v14, 32)));
        return ();
    }
}

int method_127487() impure method_id(127487) {
    var v0 = begin_parse(get_data());
    var v2 = v0~load_uint(32);
    end_parse(v0);
    return v2;
}

