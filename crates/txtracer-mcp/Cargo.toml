[package]
name = "txtracer-mcp"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
anyhow = "1.0"
tokio = { version = "1.0", features = ["full"] }
retrace = { path = "../retrace" }
tycho-types = "0.3.0"
vmlogs = { path = "../vmlogs" }
