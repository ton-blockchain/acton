// Auto-generated wrapper for contract 'GetgemsNftFixpriceSaleV3'
//
// This file is automatically generated by 'acton wrapper'
// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/getgems_nft_fixprice_sale_v3.types"

/// Storage `GetgemsNftFixpriceSaleV3Storage` is defined in `../types/getgems_nft_fixprice_sale_v3.types`
struct GetgemsNftFixpriceSaleV3 {
    address: address
    stateInit: ContractState? = null
}

/// Creates a contract wrapper instance from the storage data
fun GetgemsNftFixpriceSaleV3.fromStorage(
    storage: GetgemsNftFixpriceSaleV3Storage,
    toShard: AddressShardingOptions? = null,
): GetgemsNftFixpriceSaleV3 {
    val stateInit = ContractState {
        code: build("getgemsnftfixpricesalev3"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { stateInit, toShard }.calculateAddress();
    return GetgemsNftFixpriceSaleV3 { address, stateInit };
}

/// Creates a contract wrapper instance from the address
fun GetgemsNftFixpriceSaleV3.fromAddress(address: address): GetgemsNftFixpriceSaleV3 {
    return GetgemsNftFixpriceSaleV3 { address };
}

/// Deploys the contract to the blockchain
fun GetgemsNftFixpriceSaleV3.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: { stateInit: self.stateInit },
    });
    return net.send(from, genericMsg);
}

fun GetgemsNftFixpriceSaleV3.sendGetgemsNftFixpriceSaleV3ZeroOpcodeBuy(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GetgemsNftFixpriceSaleV3ZeroOpcodeBuy { queryId },
    });
    return net.send(from, genericMsg);
}

fun GetgemsNftFixpriceSaleV3.sendGetgemsNftFixpriceSaleV3OwnershipAssigned(
    self,
    from: address,
    queryId: uint64,
    previousOwnerAddress: address,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GetgemsNftFixpriceSaleV3OwnershipAssigned {
            queryId,
            previousOwnerAddress,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun GetgemsNftFixpriceSaleV3.sendGetgemsNftFixpriceSaleV3AcceptCoins(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GetgemsNftFixpriceSaleV3AcceptCoins { queryId },
    });
    return net.send(from, genericMsg);
}

fun GetgemsNftFixpriceSaleV3.sendGetgemsNftFixpriceSaleV3Buy(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GetgemsNftFixpriceSaleV3Buy { queryId },
    });
    return net.send(from, genericMsg);
}

fun GetgemsNftFixpriceSaleV3.sendGetgemsNftFixpriceSaleV3CancelSale(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GetgemsNftFixpriceSaleV3CancelSale { queryId },
    });
    return net.send(from, genericMsg);
}

fun GetgemsNftFixpriceSaleV3.sendGetgemsNftFixpriceSaleV3ResendRawMessage(
    self,
    from: address,
    queryId: uint64,
    resendEnvelope: GetgemsNftFixpriceSaleV3ResendEnvelope,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GetgemsNftFixpriceSaleV3ResendRawMessage {
            queryId,
            resendEnvelope: resendEnvelope.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun GetgemsNftFixpriceSaleV3.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun GetgemsNftFixpriceSaleV3.sendExternalGetgemsNftFixpriceSaleV3ExternalDeploy(
    self,
    ignoredBody: RemainingBitsAndRefs,
): ExternalSendResult {
    val body = GetgemsNftFixpriceSaleV3ExternalDeploy { ignoredBody };
    val msg = net.createExternalMessage(self.address, body);
    return net.sendExternal(msg);
}

/// Send an external-in message to the contract with a custom body cell
fun GetgemsNftFixpriceSaleV3.sendAnyExternal(self, body: cell): ExternalSendResult {
    val msg = net.createExternalMessage(self.address, body);
    return net.sendExternal(msg);
}

fun GetgemsNftFixpriceSaleV3.getSaleData(self): GetgemsNftFixpriceSaleV3SaleDataReply {
    return net.runGetMethod(self.address, "get_sale_data");
}
