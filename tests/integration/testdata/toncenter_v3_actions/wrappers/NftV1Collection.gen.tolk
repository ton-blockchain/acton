// Auto-generated wrapper for contract 'NftV1Collection'
//
// This file is automatically generated by 'acton wrapper'
// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/nft_v1_collection.types"

/// Storage `NftV1CollectionStorage` is defined in `../types/nft_v1_collection.types`
struct NftV1Collection {
    address: address
    stateInit: ContractState? = null
}

/// Creates a contract wrapper instance from the storage data
fun NftV1Collection.fromStorage(
    storage: NftV1CollectionStorage,
    toShard: AddressShardingOptions? = null,
): NftV1Collection {
    val stateInit = ContractState {
        code: build("nftv1collection"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { stateInit, toShard }.calculateAddress();
    return NftV1Collection { address, stateInit };
}

/// Creates a contract wrapper instance from the address
fun NftV1Collection.fromAddress(address: address): NftV1Collection {
    return NftV1Collection { address };
}

/// Deploys the contract to the blockchain
fun NftV1Collection.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: { stateInit: self.stateInit },
    });
    return net.send(from, genericMsg);
}

fun NftV1Collection.sendNftV1GetRoyaltyParams(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: NftV1GetRoyaltyParams { queryId },
    });
    return net.send(from, genericMsg);
}

fun NftV1Collection.sendNftV1DeployItem(
    self,
    from: address,
    queryId: uint64,
    itemIndex: uint64,
    attachTonAmount: coins,
    initParams: NftV1CollectionItemInit,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: NftV1DeployItem {
            queryId,
            itemIndex,
            attachTonAmount,
            initParams: initParams.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun NftV1Collection.sendNftV1BatchDeployItems(
    self,
    from: address,
    queryId: uint64,
    deployList: map<uint64, NftV1CollectionBatchDeployItem>,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: NftV1BatchDeployItems { queryId, deployList },
    });
    return net.send(from, genericMsg);
}

fun NftV1Collection.sendNftV1ChangeAdmin(
    self,
    from: address,
    queryId: uint64,
    newAdminAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: NftV1ChangeAdmin { queryId, newAdminAddress },
    });
    return net.send(from, genericMsg);
}

fun NftV1Collection.sendNftV1ChangeContent(
    self,
    from: address,
    queryId: uint64,
    content: NftV1CollectionContent,
    royaltyParams: NftV1RoyaltyParams,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: NftV1ChangeContent {
            queryId,
            content: content.toCell(),
            royaltyParams: royaltyParams.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun NftV1Collection.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun NftV1Collection.getCollectionData(self): GetNftV1CollectionDataReply {
    return net.runGetMethod(self.address, "get_collection_data");
}

fun NftV1Collection.getNftAddressByIndex(self, itemIndex: int): address {
    return net.runGetMethod(self.address, "get_nft_address_by_index", [itemIndex]);
}

fun NftV1Collection.royaltyParams(self): NftV1RoyaltyParams {
    return net.runGetMethod(self.address, "royalty_params");
}

fun NftV1Collection.getNftContent(self, itemIndex: int, individualContent: cell): cell {
    return net.runGetMethod(self.address, "get_nft_content", [itemIndex, individualContent]);
}
