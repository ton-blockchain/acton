// Auto-generated wrapper for contract 'WalletVesting'
//
// This file is automatically generated by 'acton wrapper'
// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/wallet_vesting.types"

/// Storage `WalletVestingStorage` is defined in `../types/wallet_vesting.types`
struct WalletVesting {
    address: address
    stateInit: ContractState? = null
}

/// Creates a contract wrapper instance from the storage data
fun WalletVesting.fromStorage(
    storage: WalletVestingStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): WalletVesting {
    val stateInit = ContractState {
        code: build("walletvesting"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return WalletVesting { address, stateInit };
}

/// Creates a contract wrapper instance from the address
fun WalletVesting.fromAddress(address: address): WalletVesting {
    return WalletVesting { address };
}

/// Deploys the contract to the blockchain
fun WalletVesting.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: { stateInit: self.stateInit },
    });
    return net.send(from, genericMsg);
}

fun WalletVesting.sendWalletVestingSend(
    self,
    from: address,
    queryId: uint64,
    sendMode: uint8,
    message: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WalletVestingSend { queryId, sendMode, message },
    });
    return net.send(from, genericMsg);
}

fun WalletVesting.sendWalletVestingAddWhitelist(
    self,
    from: address,
    queryId: uint64,
    addresses: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WalletVestingAddWhitelist { queryId, addresses },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun WalletVesting.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun WalletVesting.sendExternalWalletVestingSignedExternal(
    self,
    signature: bits512,
    subwalletId: uint32,
    validUntil: uint32,
    seqno: uint32,
    outgoing: RemainingBitsAndRefs,
): ExternalSendResult {
    val body = WalletVestingSignedExternal { signature, subwalletId, validUntil, seqno, outgoing };
    val msg = net.createExternalMessage(self.address, body);
    return net.sendExternal(msg);
}

/// Send an external-in message to the contract with a custom body cell
fun WalletVesting.sendAnyExternal(self, body: cell): ExternalSendResult {
    val msg = net.createExternalMessage(self.address, body);
    return net.sendExternal(msg);
}

fun WalletVesting.seqno(self): uint32 {
    return net.runGetMethod(self.address, "seqno");
}

fun WalletVesting.getSubwalletId(self): uint32 {
    return net.runGetMethod(self.address, "get_subwallet_id");
}

fun WalletVesting.getPublicKey(self): int {
    return net.runGetMethod(self.address, "get_public_key");
}

fun WalletVesting.getVestingData(self): GetVestingDataReply {
    return net.runGetMethod(self.address, "get_vesting_data");
}

fun WalletVesting.isWhitelisted(self, addr: address): bool {
    return net.runGetMethod(self.address, "is_whitelisted", [addr]);
}

fun WalletVesting.getWhitelist(self): lisp_list<WalletVestingWhitelistEntry> {
    return net.runGetMethod(self.address, "get_whitelist");
}

fun WalletVesting.getLockedAmount(self, atTime: int): coins {
    return net.runGetMethod(self.address, "get_locked_amount", [atTime]);
}
