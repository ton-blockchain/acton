warning[E016]: send mode should use SEND_MODE_* constants
  --> [ROOT]/contracts/main[LINE]
  |
8 |                 outMsg.send(128);
  |                             ^^^ numeric send mode literal is used here
  |
  = use named `SEND_MODE_*` constants instead of numeric mode literals

help: replace with SEND_MODE_* constants
  --> [ROOT]/contracts/main[LINE]
  |
8 |                 outMsg.send(128);
  |                             ^^^ SEND_MODE_CARRY_ALL_BALANCE

warning[E021]: send with dangerous mode requires safety comment
  --> [ROOT]/contracts/main[LINE]
  |
8 |                 outMsg.send(128);
  |                             ^^^ add `// SAFETY: ...` above this `send` call
  |
  = document why this dangerous send mode is safe in this context and which invariants are required


1 issue can be fixed automatically, rerun with --fix flag.

Use acton check --explain <CODE> to get detailed explanation of a rule.
For example: acton check --explain E016
