warning[E027]: low-level `dict` type should be replaced with typed `map<K, V>`
  --> [ROOT]/contracts/main[LINE]
  |
2 |             type StorageDict = dict
  |                                ^^^^ prefer `map<K, V>` over low-level `dict`
  |
  = Use `map<K, V>` when key and value types are known; keep `dict` only for low-level TVM interop boundaries.


Use acton check --explain <CODE> to get detailed explanation of a rule.
For example: acton check --explain E027
