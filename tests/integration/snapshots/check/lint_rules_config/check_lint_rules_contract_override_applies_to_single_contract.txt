warning[E013]: possible storage write without admin sender check
  --> [ROOT]/contracts/alpha[LINE]
  |
2 |             fun onInternalMessage(in: InMessage) {
  |                                   -- inbound sender comes from message parameter `in`
3 |                 val _sender = in.senderAddress;
4 |                 contract.setData(contract.getData());
  |                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ storage mutation (`save` / `setData`) is reachable without admin sender check
  |
  = add `assert (in.senderAddress == storage.adminAddress) throw <code>;` before storage write


Use acton check --explain <CODE> to get detailed explanation of a rule.
For example: acton check --explain E013
