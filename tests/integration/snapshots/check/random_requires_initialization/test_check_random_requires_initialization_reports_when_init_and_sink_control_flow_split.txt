warning[E018]: random generator must be initialized before `random.uint256`/`random.range` call
  --> [ROOT]/contracts/main[LINE]
  |
9 |                 val _value = random.uint256();
  |                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ `random.uint256` / `random.range` is reachable before initialization
  |
  = add `random.initialize(...)` or `random.initializeBy(...)` before this call

warning[E018]: random initialization exists, but it is not guaranteed on all execution paths
  --> [ROOT]/contracts/main[LINE]
  |
4 |                     random.initializeBy(1);
  |                     ^^^^^^^^^^^^^^^^^^^^^^ initialization is performed here


Use acton check --explain <CODE> to get detailed explanation of a rule.
For example: acton check --explain E018
