warning[S008]: negated `is` type check can use `!is`
  --> [ROOT]/contracts/main[LINE]
  |
3 |                 val b = !(((a)) is int);
  |                         ^^^^^^^^^^^^^^^ can be written with `!is`
  |
  = replace `!(a is T)` with `a !is T`

help: use `!is` operator
  --> [ROOT]/contracts/main[LINE]
  |
3 |                 val b = !(((a)) is int);
  |                         ^^^^^^^^^^^^^^^ ((a)) !is int


1 issue can be fixed automatically, rerun with --fix flag.

Use acton check --explain <CODE> to get detailed explanation of a rule.
For example: acton check --explain S008
