? Include the TypeScript app scaffold? (y/N)  ? Include the TypeScript app scaffold? (y/N) y ? Include the TypeScript app scaffold? Yes
  Installing standard library [ACTON_VERSION]
> Initialized empty Git repository in [ROOT]/foobar/.git/
✓ Created new Acton project
  Project name: interactive-counter
  Description: interactive description
  Template: counter
  TypeScript app: included
  License: MIT

Created Acton.toml with project configuration

Next steps:

  # Navigate to project
  cd [ROOT]/foobar
  # Build your contract
  acton build
  # Run tests
  acton test
  # Install app dependencies
  npm install
  # Start the TypeScript app
  npm run dev